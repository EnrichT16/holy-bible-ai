# Holy Bible · AI Assisted — backend configuration

# Your Anthropic API key (server-side only — NEVER ship this in the app)
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Optional overrides
HOLYBIBLE_MODEL=claude-sonnet-5
HOLYBIBLE_MAX_TOKENS=600
PORT=8000
